using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class githup : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("hello samiha");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
